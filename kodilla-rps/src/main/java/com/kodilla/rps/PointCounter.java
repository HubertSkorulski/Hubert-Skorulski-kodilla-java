package com.kodilla.rps;

public class PointCounter {
    /*public int countPoints (char result, int player_wins, ) {
        int player_wins = 0;
        int comp_wins = 0;

        if (result == 'W') {
            player_wins++;
        } else if (result == 'L'){
            comp_wins++;
        }

    }*/
}
